#!/bin/bash

xilution_penguin_instance_id=$1

# TODO - replace the following with a command that will remove the load_balancer_hostname reference from the Xilution load balancer lookup table.
echo "Nothing to do yet for ${xilution_penguin_instance_id}"
